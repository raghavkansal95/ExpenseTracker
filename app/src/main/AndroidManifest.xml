<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.expensetracker"
    tools:ignore="ExtraText">

    <uses-sdk tools:overrideLibrary="com.mukesh" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Activity.updateItemList"></activity>
        <activity android:name=".Activity.add_Buy_List" />
        <activity android:name=".Activity.ItemListActivity" />
        <activity android:name=".Model.BuyList" />
        <activity android:name=".Activity.EnterPhoneNumber" />

        <activity android:name=".Activity.ProfileActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".Activity.MainActivity" />
    </application>

</manifest>